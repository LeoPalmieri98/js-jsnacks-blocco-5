const numbers = [2, 8, 4, 7, 2, 87];
console.log(numbers);

const incremento1 = numbers.map ((numbers) => {
    return numbers + 1;
});
console.log(incremento1);

// Crea un nuovo array in cui tutti i numeri siano incrementati di 1
// Risultato: [3, 9, 5, 8, 3, 88]